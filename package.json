{
  "name": "playwright-tutorials",
  "version": "1.0.0",
  "description": "This is a Playwright tutorial learning repo from the YT series",
  "main": "index.js",
  "scripts": {
    "test:headed" : "npx playwright test --headed",
    "test:debug" : "npx playwright test --headed --project=chromium -ui --debug",
    "test": "npx playwright test",
    "test:chrome": "npx playwright test --project=chromium --workers=1",
    "test:firefox": "npx playwright test --project=firefox --workers=1",
    "test:webkit": "npx playwright test --project=webkit --workers=1",
    "test:show-report":"npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@types/node": "^22.13.10"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  }
}
